import { AppRegistry } from "react-native";
import App from "./App";

// You can define the app name directly in this file
const appName = "MovieApp";

// Register the main component
AppRegistry.registerComponent(appName, () => App);